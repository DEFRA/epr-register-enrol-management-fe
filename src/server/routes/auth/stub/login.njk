{% extends 'layouts/page.njk' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">Stub Login</h1>
      <p class="govuk-body">
        This is a development stub. Every caseworker has the same
        permissions (RA-323) — optionally choose a nation to log in as.
      </p>

      {% if error %}
        <div class="govuk-error-summary">
          <h2 class="govuk-error-summary__title">There is a problem</h2>
          <div class="govuk-error-summary__body">
            <ul class="govuk-list govuk-error-summary__list">
              <li>{{ error }}</li>
            </ul>
          </div>
        </div>
      {% endif %}

      <form method="POST" action="/auth/stub/login">
        <input type="hidden" name="crumb" value="{{ crumb }}" />

        <div class="govuk-form-group">
          <label class="govuk-label govuk-label--m" for="nation">
            Nation
          </label>
          <div id="nation-hint" class="govuk-hint">
            Select a nation to restrict your worklist view. Leave as "None" to see all nations.
          </div>
          <select class="govuk-select" id="nation" name="nation" aria-describedby="nation-hint">
            {% for option in nationOptions %}
              <option value="{{ option.value }}">{{ option.label }}</option>
            {% endfor %}
          </select>
        </div>

        <button class="govuk-button" type="submit">Log in</button>
      </form>

      {% if entraIdConfigured %}
        <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">
        <h2 class="govuk-heading-m">Or sign in with a real account</h2>
        <a href="/auth/regulator/entra-id" class="govuk-button govuk-button--secondary" data-testid="entra-id-login">
          Sign in with Entra ID
        </a>
      {% endif %}
    </div>
  </div>
{% endblock %}
